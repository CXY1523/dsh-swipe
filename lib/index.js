// dsh-swipe host entry: client-only plugin, the host side is a no-op placeholder.
// All behavior lives in client.js (assistant-message swipe actions).

export const name = "dsh-swipe";

export const inject = [];

export function apply() {}
